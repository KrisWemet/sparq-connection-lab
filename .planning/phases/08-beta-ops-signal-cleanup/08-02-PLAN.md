# Plan 08-02 — Re-Prove Clean Beta Ops Signal On The Primary Path

Wave: 2

Depends on:
- `08-01`

## Objective

Re-run the fresh production signup-driven walkthrough and confirm both that Day 1 still completes and that the noisy false-positive `journey_start_lookup` error row no longer appears.

## Scope

- re-run the same primary fresh-signup production walkthrough used in the current live evidence trail
- confirm the path still reaches Day 1 completion
- query `analytics_events` for the repaired run
- confirm the false-positive `journey_start_lookup` error row is absent for that successful run

## Expected Changes

- refreshed live evidence
- updated operator notes for beta ops signal trust
- no product-scope expansion

## Explicitly Out Of Scope

- new features
- broader analytics redesign
- onboarding, dashboard, or daily-growth redesign
- cleanup beyond the verified false-positive signal

## Verification

- `curl -I https://sparq-connection-lab.vercel.app/signup`
- `PLAYWRIGHT_BASE_URL=https://sparq-connection-lab.vercel.app npx playwright test e2e/tests/08-live-beta-verification.spec.ts --project=chromium --workers=1 --no-deps`
- targeted production `analytics_events` verification for the repaired run

## Success Criteria

- the primary fresh-signup production path still completes through Day 1
- the repaired successful run does not produce the false-positive `beta_primary_path_error` row for `journey_start_lookup`
- updated evidence clearly shows both path success and cleaner ops signal quality

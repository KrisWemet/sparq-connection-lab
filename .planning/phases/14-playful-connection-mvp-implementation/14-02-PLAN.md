# 14-02 Plan

Wave: 2
Goal: Add the smallest visible UI slice on safe existing surfaces without altering core dashboard or daily-growth logic.

## Milestone Plan

Render the playful MVP as optional companion surfaces:
- dashboard `Today’s Spark`
- daily-growth home companion prompt for `Favorite Us`

## UI Surfaces

- `dashboard`
  - one optional card
- `daily-growth` home
  - one optional companion card below the core step

## Files Likely Affected

- `src/pages/dashboard.tsx`
- `src/pages/daily-growth.tsx`
- `src/components/dashboard/**`
- optional new UI components under `src/components/playful/**`
- shared UI primitives only if needed for small, contained presentation

## Acceptance Criteria

- dashboard shows `Today’s Spark` only when playful content is available
- daily-growth home shows a `Favorite Us` companion prompt only when content is available
- both surfaces remain optional and non-blocking
- neither surface changes the core CTA order or meaning
- users can:
  - try
  - send
  - swap
  on the spark surface
- users can:
  - answer privately
  - send a short version
  on the `Favorite Us` surface

## Verification Plan

- visual and interaction tests for the new optional cards
- regression tests proving the existing dashboard and daily-growth core flows still behave the same
- explicit no-data tests proving the surfaces disappear cleanly without empty-state clutter

## Explicitly Out Of Scope

- new tabs
- playful feature hub
- notification center
- core dashboard layout rewrite
- core daily-growth sequence rewrite

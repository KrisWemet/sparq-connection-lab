# 15-01 Plan

Phase: 15
Wave: 1
Title: Live Deploy And Primary Path Regression Proof

## Objective

Deploy the current playful MVP slice and confirm that the proven primary signup-driven path still works end to end in live production while the new playful surfaces load correctly for authenticated users.

## Scope

- deploy the current code to production
- re-run the proven primary signup-driven walkthrough:
  - signup or register
  - onboarding
  - journey selection
  - dashboard
  - daily-growth
  - Day 1 completion
- verify `Daily Spark` loads on dashboard for an authenticated user
- verify `Favorite Us` loads on the daily-growth home for an authenticated user
- verify playful interactions work as intended:
  - swap
  - keep or save
  - copy

## Tasks

1. deploy the current playful MVP slice to production without widening scope
2. run the existing primary-path live production walkthrough against the deployed app
3. capture exact results for each primary path stage
4. verify dashboard surface behavior for `Daily Spark`
5. verify daily-growth home surface behavior for `Favorite Us`
6. capture screenshots, notes, and exact outcomes in a new live evidence file

## Acceptance Criteria

- the deployed app still supports the full primary signup-driven path through Day 1 completion
- `Daily Spark` appears on dashboard for the authenticated production user
- `Favorite Us` appears on the daily-growth home for the authenticated production user
- swap, keep or save, and copy actions work in production
- the playful slice remains optional and does not interrupt the main daily loop

## Verification

- `npm run lint`
- `npx playwright test e2e/tests/02-onboarding.spec.ts e2e/tests/03-daily-growth.spec.ts e2e/tests/05-dashboard.spec.ts e2e/tests/14-playful-connection.spec.ts --project=chromium`
- `npx vercel deploy --prod --yes`
- `curl -I https://sparq-connection-lab.vercel.app/signup`
- `PLAYWRIGHT_BASE_URL=https://sparq-connection-lab.vercel.app npx playwright test e2e/tests/08-live-beta-verification.spec.ts --project=chromium --workers=1 --no-deps`

## Notes

- do not change onboarding, Peter, dashboard core logic, or daily-growth core logic unless a real regression is found
- if a regression is found, document the exact failing stage before fixing anything

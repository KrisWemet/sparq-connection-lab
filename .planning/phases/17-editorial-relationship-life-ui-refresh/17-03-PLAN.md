---
phase: 17-editorial-relationship-life-ui-refresh
plan: 03
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - .planning/phases/17-editorial-relationship-life-ui-refresh/17-UI-SPEC.md
autonomous: false
requirements:
  - UI-EDITORIAL-01
  - UI-EDITORIAL-02
  - UI-EDITORIAL-03
must_haves:
  truths:
    - "Every safe surface (dashboard, daily-growth home, bottom navigation, playful cards) has a complete class-level spec"
    - "FavoriteUsCard has an explicit placement and wiring spec for the daily-growth home screen"
    - "The contract defines ONE featured module per screen and specifies which module is featured on each surface"
    - "The bottom navigation treatment is specified with exact class and style values"
    - "The Checker Sign-Off dimensions are evaluated and marked"
    - "The spec is actionable enough for an implementation phase without guessing"
  artifacts:
    - path: ".planning/phases/17-editorial-relationship-life-ui-refresh/17-UI-SPEC.md"
      provides: "Surface-by-surface component contract, FavoriteUsCard placement, motion contract, and Checker Sign-Off"
      contains: "FavoriteUsCard"
  key_links:
    - from: "Surface Contract section"
      to: "src/pages/Dashboard.tsx component stack"
      via: "component name references"
      pattern: "DashboardHeaderNew|DailyPulseBar|TodaysReflectionCard|ActiveChallengeCard"
    - from: "Surface Contract section"
      to: "src/pages/daily-growth.tsx component stack"
      via: "component name references"
      pattern: "DayProgressArc|TodaysExerciseCard|PreviousReflectionCard|FavoriteUsCard"
    - from: "Bottom Navigation treatment"
      to: "src/components/bottom-nav.tsx"
      via: "style and class specifications"
      pattern: "bottom-nav"
---

<objective>
Define the surface-by-surface component contract for all five safe surfaces, specify FavoriteUsCard placement and wiring, refine the motion contract, and complete the Checker Sign-Off evaluation.

Purpose: This is the final plan in the phase. It transforms the general Surface Contract section into a component-level implementation spec. Without this, the implementation phase would need to guess how each card, each gradient, each spacing value, and each animation should change. This plan makes the spec fully actionable.

Output: A complete, surface-by-surface component contract in 17-UI-SPEC.md with exact class strings, FavoriteUsCard placement, motion refinements, and a Checker Sign-Off evaluation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-editorial-relationship-life-ui-refresh/17-CONTEXT.md
@.planning/phases/17-editorial-relationship-life-ui-refresh/17-RESEARCH.md
@.planning/phases/17-editorial-relationship-life-ui-refresh/17-UI-SPEC.md
@.planning/phases/17-editorial-relationship-life-ui-refresh/17-01-SUMMARY.md
@.planning/phases/17-editorial-relationship-life-ui-refresh/17-02-SUMMARY.md
@src/pages/Dashboard.tsx
@src/pages/daily-growth.tsx
@src/components/bottom-nav.tsx
@src/components/playful/DailySparkCard.tsx
@src/components/playful/FavoriteUsCard.tsx

<interfaces>
<!-- Current Dashboard.tsx component stack (after merge conflict resolved in Plan 01) -->

Dashboard renders in order:
1. DashboardHeaderNew -- greeting + streak badge
2. DailyPulseBar -- mood selector, bg-white card
3. TodaysReflectionCard -- violet gradient, serif italic question
4. PartnerAnsweredCard (conditional)
5. Today's Focus CTA block (inline motion.div) -- dark violet gradient
6. DailySparkCard (playful, conditional) -- bg-white rounded-3xl
7. ActiveChallengeCard (upstream) -- violet gradient
8. SharedAchievements -- bg-white achievement rows

Background: inline style linear-gradient(160deg, #F5F3FF 0%, #F9FAFB 40%, #EDE9FE 100%)
Layout: max-w-lg mx-auto px-4 pt-6 space-y-5 pb-24

<!-- Current daily-growth.tsx home screen stack -->

Daily-growth home phase renders:
1. DayProgressArc (centered)
2. TodaysExerciseCard -- bg-white rounded-3xl
3. PreviousReflectionCard -- bg-[#EDE9FE]
4. Peter section -- PeterAvatar + font-serif italic tagline
[GAP: FavoriteUsCard NOT rendered -- component exists at src/components/playful/FavoriteUsCard.tsx]

Background: bg-gray-50 (WRONG -- should be bg-brand-linen)

<!-- Current bottom-nav.tsx -->

style: background rgba(255,255,255,0.95), borderTop 1px solid rgba(139,92,246,0.12), boxShadow 0 -4px 20px rgba(139,92,246,0.06)
Active: text-brand-primary + bg-brand-primary/10 pill
Inactive: text-brand-taupe/60
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define surface-by-surface component contract</name>
  <files>.planning/phases/17-editorial-relationship-life-ui-refresh/17-UI-SPEC.md</files>
  <action>
Replace the existing Surface Contract section in 17-UI-SPEC.md with a detailed, component-level implementation spec for each safe surface. Use exact Tailwind classes and brand tokens -- no vague guidance.

**SURFACE 1: Dashboard**

Page shell:

Background:  bg-brand-linen (replace inline gradient with single token)
Layout:      max-w-lg mx-auto px-4 pt-8 space-y-6 pb-28
             (change: pt-6 to pt-8 for more hero breathing room, space-y-5 to space-y-6)

Component stack (in render order):

| # | Component | Role | Featured? | Container Classes | Key Changes |
|---|-----------|------|-----------|-------------------|-------------|
| 1 | DashboardHeaderNew | Hero greeting | Display heading | No card container -- flush left in page padding | Greeting text becomes Display role: `font-serif font-semibold text-[40px] leading-[1.05] tracking-tight text-brand-espresso`. Streak badge uses Eyebrow role. |
| 2 | DailyPulseBar | Mood check-in | No | `bg-brand-parchment rounded-3xl border border-brand-primary/10 shadow-sm p-5` | Replace `bg-white`. |
| 3 | TodaysReflectionCard | Emotional pull-quote | YES (featured) | `bg-gradient-to-br from-brand-primary to-brand-hover rounded-3xl shadow-md p-6` | This is the ONE featured card on dashboard. Keep serif italic white text. All other cards must be parchment. |
| 4 | PartnerAnsweredCard | Partner nudge | No | `bg-brand-parchment rounded-3xl border border-brand-primary/10 shadow-sm p-5` | Replace gradient with parchment. |
| 5 | Today's Focus CTA | Primary action | No | `bg-brand-parchment rounded-3xl border border-brand-primary/10 shadow-sm p-5` | Demote from dark gradient to standard card. CTA button stays `bg-brand-primary text-white rounded-2xl`. The focus block should feel like a clear next step, not a competing hero. |
| 6 | DailySparkCard | Playful prompt | No | `bg-brand-parchment rounded-3xl border border-brand-primary/10 shadow-sm p-5` | Replace `bg-white`. Keep serif italic prompt text. |
| 7 | ActiveChallengeCard | Journey progress | No | `bg-brand-parchment rounded-3xl border border-brand-primary/10 shadow-sm p-5` | Demote from gradient to standard card. Progress ring stays `brand-primary`. |
| 8 | SharedAchievements | Milestone display | No | `bg-brand-parchment rounded-3xl border border-brand-primary/10 shadow-sm p-5` | Replace `bg-white`. |

Hierarchy rule: TodaysReflectionCard is the only featured (gradient) card. Every other card is a parchment editorial card. This creates clear visual hierarchy instead of a wall of purple gradients.

**SURFACE 2: Daily-Growth Home**

Page shell:

Background:  bg-brand-linen (replace bg-gray-50)
Layout:      max-w-lg mx-auto px-4 pt-8 space-y-6 pb-28

Component stack (in render order):

| # | Component | Role | Featured? | Container Classes | Key Changes |
|---|-----------|------|-----------|-------------------|-------------|
| 0 | Hero statement | Screen title | Display heading | No card container -- flush left in page padding | NEW -- add a Display-role heading: `font-serif font-semibold text-[40px] leading-[1.05] tracking-tight text-brand-espresso`. Content: "Your practice for today" |
| 1 | DayProgressArc | Day counter | No | No card container -- centered inline element | Keep current styling. |
| 2 | TodaysExerciseCard | Core daily action | YES (featured) | `bg-gradient-to-br from-brand-primary to-brand-hover rounded-3xl shadow-md p-6` | Promote to featured card. This is the ONE action card on this screen. All text shifts to white on gradient. |
| 3 | PreviousReflectionCard | Yesterday's reflection | No | `bg-brand-parchment rounded-3xl border border-brand-primary/10 shadow-sm p-5` | Already close -- normalize inline hex to brand token. |
| 4 | FavoriteUsCard | Playful prompt (couple) | No | `bg-brand-parchment rounded-3xl border border-brand-primary/10 shadow-sm p-5` | **NEW PLACEMENT** -- render between PreviousReflectionCard and Peter section. See Task 2 for wiring spec. |
| 5 | Peter section | Companion presence | No | No card container -- centered, subtle | Keep current PeterAvatar + tagline. Normalize tagline to `font-serif italic text-sm leading-normal text-brand-taupe`. |

**SURFACE 3: Bottom Navigation**

Treatment: softened floating tray

Replace the current inline style with Tailwind classes where possible, keeping the floating glass feel:

Container:
  Current:  background rgba(255,255,255,0.95), inline borderTop, inline boxShadow
  Target:   bg-white/95 backdrop-blur-md border-t border-brand-primary/10
            shadow: 0 -2px 12px rgba(139,92,246,0.05)  (softer than current -4px 20px)
            rounded-t-2xl  (NEW -- soft top corners for floating tray feel)
            mx-2 mb-0  (NEW -- slight horizontal inset for cushioned-well appearance)

Active state:
  Current:  text-brand-primary + bg-brand-primary/10 pill
  Target:   Keep -- this is already correct

Inactive state:
  Current:  text-brand-taupe/60
  Target:   Keep -- this is already correct

Icon sizing:
  Current:  22px (inferred from Lucide default)
  Target:   22px nav icons, 18px inline icons, 16px small -- keep current convention

The navigation label text stays at `text-[10px]` with `font-semibold tracking-wide`. Active labels: `text-brand-primary`. Inactive labels: `text-brand-taupe/60`.

Do NOT change navigation routes, item order, or hide/show logic.

**SURFACE 4: Playful Cards (DailySparkCard + FavoriteUsCard)**

Both playful cards follow the standard parchment editorial card template:

Container:  bg-brand-parchment rounded-3xl border border-brand-primary/10 shadow-sm p-5
Eyebrow:    font-sans font-semibold text-[11px] tracking-[0.06em] uppercase text-brand-taupe mb-2
Prompt:     font-serif italic text-lg leading-snug text-brand-espresso
Action row: mt-4 flex gap-3
Buttons:    rounded-2xl px-4 py-2 text-sm font-semibold

Primary change: Replace `bg-white` with `bg-brand-parchment` on both cards.

DailySparkCard eyebrow: "A little spark"
FavoriteUsCard eyebrow: "Something to share"

Keep all existing swap, save, and copy interactions unchanged. No behavioral modifications.
  </action>
  <verify>
    <automated>cd /Users/chris/sparq-connection-lab && grep -c "DashboardHeaderNew\|DailyPulseBar\|TodaysReflectionCard\|TodaysExerciseCard\|FavoriteUsCard\|ActiveChallengeCard\|SharedAchievements\|DailySparkCard\|PreviousReflectionCard\|bottom-nav\|bottom.nav" .planning/phases/17-editorial-relationship-life-ui-refresh/17-UI-SPEC.md; echo "Expected: 10+ (all components referenced)"</automated>
  </verify>
  <done>Every component on every safe surface has exact container classes, role assignment, and key changes documented. One featured card per screen is declared. The bottom navigation treatment is specified. All bg-white targets are replaced with bg-brand-parchment. All inline hex targets use brand tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Define FavoriteUsCard placement and wiring spec</name>
  <files>.planning/phases/17-editorial-relationship-life-ui-refresh/17-UI-SPEC.md</files>
  <action>
Add a focused subsection under the Daily-Growth Home surface contract called "### FavoriteUsCard Wiring Specification" that gives the implementation phase everything it needs.

**Current state:** FavoriteUsCard exists at `src/components/playful/FavoriteUsCard.tsx`. The playful API (`/api/playful/today`) already delivers `favorite_us` prompt data. The component is fully implemented and styled. However, it is NOT imported or rendered in `src/pages/daily-growth.tsx`.

**Implementation requirement the spec must define:**

1. In `src/pages/daily-growth.tsx`, import FavoriteUsCard:
   `import FavoriteUsCard from '@/components/playful/FavoriteUsCard';`

2. Render FavoriteUsCard in the home phase component stack, AFTER PreviousReflectionCard and BEFORE the Peter section.

3. The card should only render when:
   - The user is in the `home` phase (not during an active session step)
   - The playful API returned a `favorite_us` prompt (fail-soft: if no prompt, do not render)
   - The card already handles its own loading/error states internally

4. Wrap in the standard Framer Motion entrance:
   `motion.div` with `initial={{ opacity: 0, y: 12 }}`, `animate={{ opacity: 1, y: 0 }}`, `transition={{ duration: 0.4, delay: 0.2 }}`

5. The delay (0.2s) staggers it after PreviousReflectionCard to avoid simultaneous entrance.

**Styling:** FavoriteUsCard already uses the correct playful card pattern. The implementation phase should update its container from `bg-white` to `bg-brand-parchment` as specified in the Playful Cards section.

**Data flow:** FavoriteUsCard already fetches its own data from `/api/playful/today`. No new data plumbing is needed in daily-growth.tsx. The component is self-contained.

This closes the gap identified in 17-RESEARCH.md Pitfall 3 and documented in the RESEARCH.md Common Pitfalls section.
  </action>
  <verify>
    <automated>cd /Users/chris/sparq-connection-lab && grep -c "FavoriteUsCard Wiring" .planning/phases/17-editorial-relationship-life-ui-refresh/17-UI-SPEC.md; echo "Expected: 1 (section present)"</automated>
  </verify>
  <done>The FavoriteUsCard wiring spec includes: exact import path, render position in the component stack, conditional rendering rules (home phase + prompt available), Framer Motion entrance with stagger delay, styling token update, and confirmation that no new data plumbing is needed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human review of complete editorial UI design contract</name>
  <files>.planning/phases/17-editorial-relationship-life-ui-refresh/17-UI-SPEC.md</files>
  <action>
Present the completed 17-UI-SPEC.md for Chris to review. The full Phase 17 editorial UI design contract has been produced across three plans:

1. Plan 01 (complete): Dashboard.tsx merge conflict resolved. UI-SPEC.md color table corrected from warm ivory to live lavender palette. CSS variable divergence documented.

2. Plan 02 (complete): Seven-role type hierarchy with exact Tailwind classes. Per-surface type usage map. Editorial card template (standard + featured). Expanded copywriting contract with forbidden patterns.

3. Plan 03 (this plan): Surface-by-surface component contract with exact class strings for every component on every surface. FavoriteUsCard placement and wiring spec. Bottom navigation softened tray treatment. One-featured-card-per-screen hierarchy rule. Motion contract. Checker sign-off.

The full spec is now in `.planning/phases/17-editorial-relationship-life-ui-refresh/17-UI-SPEC.md`.
  </action>
  <what-built>
The complete Phase 17 editorial UI design contract across all three plans, resulting in an updated 17-UI-SPEC.md that covers: authoritative lavender color palette, seven-role type hierarchy, per-surface component contracts with exact Tailwind classes, FavoriteUsCard wiring specification, bottom navigation floating tray treatment, copywriting contract with forbidden patterns, and editorial card template system.
  </what-built>
  <how-to-verify>
Review the updated 17-UI-SPEC.md:

1. Open `.planning/phases/17-editorial-relationship-life-ui-refresh/17-UI-SPEC.md`
2. Verify the Color section uses ONLY the live tailwind.config.ts values (#F5F3FF, #EDE9FE, #8B5CF6) -- no warm ivory
3. Verify the Typography section has 7 roles with Tailwind class strings
4. Verify the Surface Contract covers: Dashboard (8 components), Daily-Growth Home (6 components), Bottom Navigation, Playful Cards
5. Verify FavoriteUsCard has a wiring spec with import path, render position, and conditional logic
6. Verify each surface has exactly ONE featured card (gradient) and all others are parchment
7. Verify the Copywriting Contract includes forbidden patterns and product voice rules
8. Verify "Lora" appears as the serif font -- "Cormorant Garamond" does NOT appear
9. Check that the overall contract feels like it could drive an implementation phase without questions

Does this contract match your editorial vision for Sparq? Are there any surfaces, components, or styling decisions that need adjustment before implementation?
  </how-to-verify>
  <verify>Visual review of 17-UI-SPEC.md by Chris</verify>
  <done>Chris approves the contract as ready for the implementation phase, or provides specific changes needed.</done>
  <resume-signal>Type "approved" to complete Phase 17, or describe any changes needed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spec-to-implementation handoff | The spec defines class strings and inline styles that the implementation phase will apply. If the spec contained XSS-vulnerable patterns (e.g., user-controlled style values), those would propagate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Injection | Spec class strings | accept | All class strings use static Tailwind tokens and brand color references -- no user input, no dynamic style injection. Implementation phase should not interpolate user data into className strings. |
| T-17-06 | Tampering | FavoriteUsCard wiring spec | accept | The wiring spec references an existing component and existing API endpoint. No new data flow is introduced. The component already handles its own auth and data fetching. |
| T-17-07 | Information Disclosure | Surface component inventory | accept | The spec documents component names and render order -- this is architecture documentation, not runtime secrets. The spec lives in .planning/, not deployed. |
</threat_model>

<verification>
1. Every safe surface has a component-level table with exact Tailwind classes
2. FavoriteUsCard wiring spec exists with import path, render position, and conditional logic
3. ONE featured card declared per screen (TodaysReflectionCard on dashboard, TodaysExerciseCard on daily-growth)
4. Bottom navigation has exact style/class targets
5. All bg-white in target columns are replaced with bg-brand-parchment
6. All text-gray-* in target columns are replaced with brand tokens
7. Motion contract is present with entrance timing
8. Checker Sign-Off dimensions are evaluated
9. Human review confirms the contract matches editorial vision
</verification>

<success_criteria>
- Every component on every safe surface has implementation-ready class strings
- FavoriteUsCard placement gap is closed with a complete wiring spec
- One featured card per screen -- clear visual hierarchy
- Bottom navigation has a softened floating tray treatment
- No bg-white, no text-gray-*, no raw hex in any target pattern
- The Checker Sign-Off is evaluated for all six dimensions
- Chris approves the contract as ready for the implementation phase
</success_criteria>

<output>
After completion, create `.planning/phases/17-editorial-relationship-life-ui-refresh/17-03-SUMMARY.md`
</output>

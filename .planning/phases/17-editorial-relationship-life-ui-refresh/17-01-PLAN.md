---
phase: 17-editorial-relationship-life-ui-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/17-editorial-relationship-life-ui-refresh/17-UI-SPEC.md
  - src/pages/Dashboard.tsx
autonomous: true
requirements:
  - UI-EDITORIAL-01
  - UI-EDITORIAL-03
must_haves:
  truths:
    - "The UI-SPEC.md color table uses the live lavender palette (#F5F3FF, #EDE9FE, #8B5CF6) — no warm ivory (#F7F1EC) or draft violet (#6E56F7) values remain"
    - "Dashboard.tsx has no merge conflict markers and builds cleanly"
    - "The UI-SPEC.md declares a single authoritative design token table that matches tailwind.config.ts"
    - "The CSS variable divergence (--background, --card) is documented with target values for the implementation phase"
  artifacts:
    - path: ".planning/phases/17-editorial-relationship-life-ui-refresh/17-UI-SPEC.md"
      provides: "Authoritative design token contract with lavender palette, reconciled CSS variables, and complete color allocation table"
      contains: "#F5F3FF"
    - path: "src/pages/Dashboard.tsx"
      provides: "Clean dashboard file with merge conflict resolved (upstream taken)"
  key_links:
    - from: "17-UI-SPEC.md Color section"
      to: "tailwind.config.ts brand tokens"
      via: "exact hex match"
      pattern: "#8B5CF6|#F5F3FF|#EDE9FE"
---

<objective>
Resolve the Dashboard.tsx merge conflict and establish the authoritative design token contract by correcting the UI-SPEC.md color table from warm ivory draft values to the live lavender palette.

Purpose: Before any per-surface or typography contract work can proceed, the spec must match the live codebase, the build must be clean, and the CSS variable divergence must be documented. This is the foundation for plans 02 and 03.

Output: A clean Dashboard.tsx (merge conflict resolved), and an updated 17-UI-SPEC.md with the authoritative color/token table aligned to tailwind.config.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-editorial-relationship-life-ui-refresh/17-CONTEXT.md
@.planning/phases/17-editorial-relationship-life-ui-refresh/17-RESEARCH.md
@.planning/phases/17-editorial-relationship-life-ui-refresh/17-UI-SPEC.md
@tailwind.config.ts
@src/styles/globals.css
@src/pages/Dashboard.tsx

<interfaces>
<!-- Authoritative token values from tailwind.config.ts — the executor must use these as the single source of truth -->

From tailwind.config.ts:
```typescript
brand: {
  primary: "#8B5CF6",   // Soft violet
  hover: "#7C3AED",     // Violet-600 hover
  light: "#FAF9FF",     // Barely-there lavender white
  linen: "#F5F3FF",     // Soft lavender page background
  parchment: "#EDE9FE", // Lavender card surfaces
  card: "#EDE9FE",      // Alias for parchment
  espresso: "#2E1065",  // Deep violet near-black for headings
  sand: "#F9C74F",      // Butter gold for highlights/streaks
  taupe: "#6B5B9E",     // Muted purple for secondary text
  growth: "#93C5FD",    // Soft sky blue for progress
}
```

From globals.css (CSS variables that DIVERGE from brand tokens — must be documented for implementation fix):
```css
--background: 220 14% 96%;  /* Cool grey — should become brand.linen HSL */
--card: 0 0% 100%;          /* Pure white — should become brand.parchment HSL */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve Dashboard.tsx merge conflict</name>
  <files>src/pages/Dashboard.tsx</files>
  <action>
Open `src/pages/Dashboard.tsx` and find the merge conflict markers starting at line 243 (`<<<<<<< Updated upstream`). Resolve by taking the upstream version:
- Keep `ActiveChallengeCard` and its import
- Discard the incoming stash content (`IdentityArcCard` and any other stash-only components)
- Remove ALL conflict markers (`<<<<<<<`, `=======`, `>>>>>>>`)
- Verify the file parses as valid TypeScript after resolution
- Do NOT make any other changes to Dashboard.tsx — no styling, no reordering, no cleanup

This is explicitly directed by STATE.md: "resolve the dashboard.tsx merge conflict (take upstream)".
  </action>
  <verify>
    <automated>cd /Users/chris/sparq-connection-lab && npx tsc --noEmit src/pages/Dashboard.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Dashboard.tsx has zero merge conflict markers, zero TypeScript errors, and the upstream ActiveChallengeCard is rendered. No other changes made.</done>
</task>

<task type="auto">
  <name>Task 2: Correct UI-SPEC.md color table and establish authoritative token contract</name>
  <files>.planning/phases/17-editorial-relationship-life-ui-refresh/17-UI-SPEC.md</files>
  <action>
Rewrite the Color section of `17-UI-SPEC.md` to replace draft values with the live codebase palette. Per user locked decision: the background color is Lavender #F5F3FF — keep the violet palette as-is in tailwind.config.ts.

**Replace the Color table** (currently at line 63-70) with the authoritative values:

| Role | Token | Value | Usage |
|------|-------|-------|-------|
| Dominant (60%) | `brand.linen` / `bg-brand-linen` | `#F5F3FF` | App background, main page canvas, quiet surfaces |
| Secondary (30%) | `brand.parchment` / `bg-brand-parchment` | `#EDE9FE` | Card surfaces, secondary modules, soft highlight zones |
| Accent (10%) | `brand.primary` / `bg-brand-primary` | `#8B5CF6` | Primary CTA fills, active nav state, progress rings, selected chips, one featured card per screen |
| Accent hover | `brand.hover` | `#7C3AED` | Hover state for accent elements |
| Near-white | `brand.light` | `#FAF9FF` | Barely-there lavender white, subtle tints |
| Destructive | — | `#C95B6A` | Destructive actions only |

**Replace the Supporting neutrals** block with:

| Role | Token | Value | Usage |
|------|-------|-------|-------|
| Text primary | `brand.espresso` / `text-brand-espresso` | `#2E1065` | Headings, hero text, dark UI elements |
| Text secondary | `brand.taupe` / `text-brand-taupe` | `#6B5B9E` | Captions, secondary copy, inactive labels |
| Border soft | `border-brand-primary/10` | `rgba(139,92,246,0.1)` | Warm subtle card borders |
| Highlight / streak | `brand.sand` / `text-brand-sand` | `#F9C74F` | Celebrations, streaks, achievement moments |
| Growth / progress | `brand.growth` / `text-brand-growth` | `#93C5FD` | Progress bars, completion states |

**Also update the Font line** in the Design System table (line 24): replace "Cormorant Garamond or equivalent" with "Lora (loaded via next/font/google as `--font-serif`)" — per user locked decision that Lora is the display serif.

**Add a new section** after the Color section called "## CSS Variable Reconciliation (Implementation Phase)":

```
## CSS Variable Reconciliation (Implementation Phase)

The following shadcn CSS custom properties in `src/styles/globals.css` currently diverge from the brand token palette. The implementation phase MUST update them:

| CSS Variable | Current Value (HSL) | Current Rendered | Target Value (HSL) | Target Rendered | Notes |
|---|---|---|---|---|---|
| `--background` | `220 14% 96%` | Cool grey `#F0F1F3` | `250 100% 98%` | `#F5F3FF` (brand.linen) | Components using `bg-background` will shift from grey to warm lavender |
| `--card` | `0 0% 100%` | Pure white `#FFFFFF` | `250 89% 96%` | `#EDE9FE` (brand.parchment) | Components using `bg-card` will shift from white to lavender card |
| `--border` | `220 13% 91%` | Cool grey border | `263 70% 63% / 0.1` | Matches `border-brand-primary/10` | OR keep as-is and use `border-brand-primary/10` directly on card containers |
```

**Remove any remaining references** to `#F7F1EC`, `#EEE7F8`, `#6E56F7`, `#2A2234`, `#655D73`, or `#DED6EA` — these are all draft values that do not exist in tailwind.config.ts.

**Keep unchanged:** the Spacing Scale section, the Motion Contract section, the Registry Safety section, and the Checker Sign-Off section.
  </action>
  <verify>
    <automated>cd /Users/chris/sparq-connection-lab && grep -c "F7F1EC\|EEE7F8\|6E56F7\|2A2234\|655D73\|DED6EA" .planning/phases/17-editorial-relationship-life-ui-refresh/17-UI-SPEC.md; echo "Expected: 0 (all draft values removed)"</automated>
  </verify>
  <done>The UI-SPEC.md Color section uses exclusively live tailwind.config.ts values. No warm ivory or draft violet hex codes remain. The CSS variable reconciliation section documents exactly what the implementation phase must fix. The font reference says Lora, not Cormorant Garamond.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan modifies a spec document and resolves a merge conflict. No new data inputs, API routes, or user-facing form changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | Dashboard.tsx merge conflict resolution | accept | Merge conflict resolution is a one-time git operation on a tracked file; no user input involved |
| T-17-02 | Information Disclosure | UI-SPEC.md token values | accept | Design tokens are non-sensitive visual configuration; the spec file is in the planning directory, not deployed |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes with no errors (proves merge conflict is resolved)
2. `npm run lint` passes (no conflict markers in source)
3. `grep -c "F7F1EC\|EEE7F8\|6E56F7" 17-UI-SPEC.md` returns 0 (all draft values gone)
4. `grep -c "F5F3FF\|EDE9FE\|8B5CF6" 17-UI-SPEC.md` returns 3+ (live values present)
5. The UI-SPEC.md font reference says "Lora" not "Cormorant Garamond"
</verification>

<success_criteria>
- Dashboard.tsx builds cleanly with zero merge conflict markers
- UI-SPEC.md Color section matches tailwind.config.ts exactly
- CSS variable divergence is documented with target HSL values for the implementation phase
- Lora is declared as the editorial display serif
- No warm ivory, no Cormorant Garamond, no draft hex values remain
</success_criteria>

<output>
After completion, create `.planning/phases/17-editorial-relationship-life-ui-refresh/17-01-SUMMARY.md`
</output>

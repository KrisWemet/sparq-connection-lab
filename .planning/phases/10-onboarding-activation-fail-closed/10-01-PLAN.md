# 10-01 Plan

Wave: 1
Goal: Make the onboarding journey-confirmation handoff fail closed without redesigning onboarding.

## Scope

- `src/components/onboarding/JourneyDetail.tsx`
- narrow supporting tests for the onboarding confirmation path

## Tasks

1. Inspect the current confirmation sequence in `JourneyDetail.handleStart()`.
2. Make starter journey activation require a successful HTTP response before continuing.
3. Make the `profiles` update require a successful Supabase response before continuing.
4. Ensure `onConfirm()` is called only after both writes succeed.
5. Emit a narrow primary-path error event with enough context to distinguish:
   - journey activation failure
   - onboarding persistence failure
6. Keep the visible onboarding structure and copy unchanged unless a tiny error-state clarification is required.
7. Add or update focused automated coverage for the fail-closed handoff behavior.

## Verification

- `npm run lint`
- focused Playwright or component coverage for the onboarding confirmation path
- existing onboarding and daily-loop regression specs that cover the primary path locally

## Done When

- failed activation no longer advances the user
- failed `isonboarded` write no longer advances the user
- success still advances exactly once
- a narrow primary-path error event is emitted on either failure path

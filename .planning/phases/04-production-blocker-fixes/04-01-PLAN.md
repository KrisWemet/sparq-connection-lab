---
phase: 04-production-blocker-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/signup.tsx
  - src/components/auth
  - src/lib/auth-context.tsx
  - src/lib/auth
  - e2e/tests/08-live-beta-verification.spec.ts
  - .planning/phases/04-production-blocker-fixes
autonomous: true
requirements:
  - BETA-PROD-SIGNUP-01
user_setup: []
must_haves:
  truths:
    - The production `/signup` route loads without HTTP 500.
    - The fix is driven by Phase 3 evidence, not by speculative cleanup.
    - Verification includes a live check against the deployed route after the fix ships.
  artifacts:
    - src/pages/signup.tsx
    - .planning/phases/04-production-blocker-fixes
  key_links:
    - The supported solo-first signup path is restored without expanding scope.
---

<objective>
Fix the live `/signup` production blocker proven in Phase 3.

Purpose: Public beta cannot start while the supported signup route hard-fails.
Output: A narrow code fix for the live signup error plus updated live verification evidence showing the route now loads.
</objective>

<context>
@SPARQ_MASTER_SPEC.md
@.planning/phases/03-live-beta-verification/03-LIVE-EVIDENCE.md
@LAUNCH_CHECKLIST.md
@IMPLEMENTATION_STATUS.md
@src/pages/signup.tsx
@src/components/auth/LoginForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose and fix the live `/signup` route</name>
  <files>src/pages/signup.tsx, src/components/auth, src/lib/auth-context.tsx, src/lib/auth</files>
  <action>Trace the production-only failure path on `/signup`, identify the smallest fix that stops the HTTP 500, and implement only the changes required to restore the supported signup route. Keep the work narrow to the blocker proven in `03-LIVE-EVIDENCE.md`.</action>
  <verify>local and live checks show `/signup` no longer returns 500</verify>
  <acceptance_criteria>
    - `/signup` loads in production
    - the fix stays tightly scoped to the proven blocker
    - no unrelated auth cleanup is bundled in
  </acceptance_criteria>
  <done>The live signup route is restored.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] local verification for the signup route passes
- [ ] live route check for `/signup` no longer returns 500
- [ ] evidence is captured in a durable Phase 4 artifact
</verification>

<output>
After completion, create `.planning/phases/04-production-blocker-fixes/04-01-SUMMARY.md`
</output>

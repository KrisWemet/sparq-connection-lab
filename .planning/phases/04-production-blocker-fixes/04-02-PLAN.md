---
phase: 04-production-blocker-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/pages/daily-growth.tsx
  - src/pages/onboarding.tsx
  - src/pages/api/preferences.ts
  - src/lib/auth-context.tsx
  - e2e/tests/09-live-login-fallback.spec.ts
  - .planning/phases/04-production-blocker-fixes
autonomous: true
requirements:
  - BETA-PROD-DAILY-01
  - BETA-PROD-VERIFY-01
user_setup: []
must_haves:
  truths:
    - A fresh solo user can reach the Day 1 morning flow from the supported live path.
    - The fix is constrained to the consent-gated daily entry blocker proven in Phase 3.
    - The same live evidence checks are re-run after the fix.
  artifacts:
    - src/pages/daily-growth.tsx
    - e2e/tests/09-live-login-fallback.spec.ts
    - .planning/phases/04-production-blocker-fixes
  key_links:
    - The repaired daily entry path is proven against the same production baseline that previously failed.
---

<objective>
Fix the live `/daily-growth` entry blocker for fresh solo users, then re-run the Phase 3 live evidence checks.

Purpose: Even with signup restored, the beta is still blocked if fresh users cannot reach Day 1 morning flow.
Output: A narrow daily-entry fix plus renewed live verification showing whether both production blockers are cleared.
</objective>

<context>
@SPARQ_MASTER_SPEC.md
@.planning/phases/03-live-beta-verification/03-LIVE-EVIDENCE.md
@LAUNCH_CHECKLIST.md
@IMPLEMENTATION_STATUS.md
@src/pages/daily-growth.tsx
@src/pages/onboarding.tsx
@e2e/tests/08-live-beta-verification.spec.ts
@e2e/tests/09-live-login-fallback.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the fresh-user daily entry blocker</name>
  <files>src/pages/daily-growth.tsx, src/pages/onboarding.tsx, src/pages/api/preferences.ts, src/lib/auth-context.tsx</files>
  <action>Trace why a fresh solo user is reaching a consent gate instead of the Day 1 morning flow on `/daily-growth`, then implement the smallest production-safe fix that aligns the live behavior with the supported beta path. Do not redesign consent or onboarding beyond what is required to remove the blocker.</action>
  <verify>fresh-user path reaches Day 1 morning flow in local and live verification</verify>
  <acceptance_criteria>
    - fresh solo user can reach the expected Day 1 morning state
    - fix stays narrow to the proven blocker
    - no unrelated daily-loop or onboarding scope is introduced
  </acceptance_criteria>
  <done>The live daily entry blocker is removed.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the existing live evidence checks</name>
  <files>e2e/tests/08-live-beta-verification.spec.ts, e2e/tests/09-live-login-fallback.spec.ts, .planning/phases/04-production-blocker-fixes, LAUNCH_CHECKLIST.md, IMPLEMENTATION_STATUS.md</files>
  <action>Re-run the same live checks used in Phase 3 against production after the fixes ship: route check for `/signup`, live browser walkthrough, and fallback live path. Record the new outcomes beside the old evidence so the blocker status is explicit.</action>
  <verify>live checks are rerun and new evidence is captured</verify>
  <acceptance_criteria>
    - Phase 3 checks are rerun, not replaced with weaker checks
    - evidence clearly shows whether the blockers are resolved
    - docs reflect the new live status
  </acceptance_criteria>
  <done>Production blocker fixes are re-verified against the live deployment.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `/signup` live route check passes
- [ ] live fresh-user path reaches Day 1 morning flow
- [ ] updated live evidence is captured in a durable file
- [ ] `LAUNCH_CHECKLIST.md` and `IMPLEMENTATION_STATUS.md` reflect the post-fix live status
</verification>

<output>
After completion, create `.planning/phases/04-production-blocker-fixes/04-02-SUMMARY.md`
</output>

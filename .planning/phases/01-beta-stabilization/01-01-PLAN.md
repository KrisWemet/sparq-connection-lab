---
phase: 01-beta-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/auth-context.tsx
  - src/lib/supabase.ts
  - src/lib/server/supabase-auth.ts
  - src/hooks/useAuth.ts
  - src/hooks/useAuth.tsx
  - src/components/ProtectedRoute.tsx
  - src/components/auth/ProtectedRoute.tsx
  - src/components/ui/protected-route.tsx
  - e2e/auth.setup.ts
  - e2e/tests/01-auth.spec.ts
autonomous: true
requirements:
  - BETA-AUTH-01
  - BETA-ARCH-01
  - BETA-TEST-01
user_setup: []
must_haves:
  truths:
    - There is one clear beta-path auth provider and one clear API auth helper.
    - Browser session persistence and auth bootstrap behave consistently across reloads and Playwright setup.
    - Duplicate auth entry points no longer create ambiguity for the critical beta path.
  artifacts:
    - src/lib/auth-context.tsx
    - src/lib/server/supabase-auth.ts
    - e2e/auth.setup.ts
    - e2e/tests/01-auth.spec.ts
  key_links:
    - Protected routes and page auth checks resolve through the canonical auth provider.
    - API routes continue to authenticate through getAuthedContext with no parallel beta-path alternative.
---

<objective>
Stabilize the auth foundation for beta by making the canonical login, session, and route-protection path explicit.

Purpose: Auth inconsistency poisons onboarding and daily-loop reliability. This plan reduces that risk first.
Output: A clearly canonical auth path, reduced duplication around beta-critical auth helpers, and verification that signup/login/session reuse stay stable.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-beta-stabilization/01-CONTEXT.md
@SPARQ_MASTER_SPEC.md
@IMPLEMENTATION_STATUS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@src/lib/auth-context.tsx
@src/lib/server/supabase-auth.ts
@src/lib/supabase.ts
@e2e/auth.setup.ts
@e2e/tests/01-auth.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize the beta auth path</name>
  <files>src/lib/auth-context.tsx, src/lib/supabase.ts, src/lib/server/supabase-auth.ts, src/hooks/useAuth.ts, src/hooks/useAuth.tsx</files>
  <read_first>SPARQ_MASTER_SPEC.md, IMPLEMENTATION_STATUS.md, .planning/codebase/ARCHITECTURE.md</read_first>
  <action>Audit the beta-critical auth path and consolidate it around `src/lib/auth-context.tsx` for browser state and `src/lib/server/supabase-auth.ts` for API auth. Either remove direct usage of duplicate hooks from the active beta path or replace them with thin re-exports that point to the canonical implementation. Keep browser session persistence behavior explicit in `src/lib/supabase.ts` and ensure comments and exports make the supported path unambiguous.</action>
  <verify>rg -n "useAuth" src | sed -n '1,80p' && npm run lint</verify>
  <acceptance_criteria>
    - `src/lib/auth-context.tsx` remains the obvious primary auth provider for the app shell
    - duplicate `useAuth` hooks no longer represent different live implementations for the beta path
    - `src/lib/server/supabase-auth.ts` remains the single API auth gate used by critical routes
  </acceptance_criteria>
  <done>Critical auth files and exports point to one beta-safe implementation path.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce protected-route and auth-bootstrap ambiguity</name>
  <files>src/components/ProtectedRoute.tsx, src/components/auth/ProtectedRoute.tsx, src/components/ui/protected-route.tsx, e2e/auth.setup.ts, e2e/tests/01-auth.spec.ts</files>
  <read_first>IMPLEMENTATION_STATUS.md, .planning/codebase/TESTING.md</read_first>
  <action>Collapse protected-route behavior so the active beta path uses one canonical component or one canonical export surface. Update Playwright auth bootstrap and auth flow tests so they verify the same supported path the app uses in production, including signup or login, consent persistence where required, session reuse, and post-auth redirects.</action>
  <verify>npx playwright test e2e/tests/01-auth.spec.ts</verify>
  <acceptance_criteria>
    - only one protected-route implementation is needed for beta-critical pages, or all duplicates become explicit wrappers around the canonical version
    - auth setup creates reusable storage state without relying on brittle route assumptions
    - auth spec passes against the canonical path
  </acceptance_criteria>
  <done>Protected-route usage and Playwright bootstrap match the real beta auth flow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes or reports only pre-existing unrelated warnings
- [ ] `npx playwright test e2e/tests/01-auth.spec.ts` passes
- [ ] Reloaded browser sessions remain authenticated on the beta path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No new auth path ambiguity is introduced
- Auth behavior is stable enough to support onboarding and daily-loop work in later plans
  </success_criteria>

<output>
After completion, create `.planning/phases/01-beta-stabilization/01-01-SUMMARY.md`
</output>

---
phase: 02-beta-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LAUNCH_CHECKLIST.md
  - IMPLEMENTATION_STATUS.md
  - supabase/schema.sql
  - supabase/migrations
  - src/lib/api-config.ts
  - src/integrations/supabase/client.ts
  - scripts
  - .env.example
autonomous: true
requirements:
  - BETA-HARD-DB-01
  - BETA-HARD-LAUNCH-01
  - BETA-HARD-TEST-01
user_setup:
  - Active Supabase and deployment credentials may be required for live-environment verification beyond local repo checks.
must_haves:
  truths:
    - The beta path no longer depends on ambiguous environment-variable sources.
    - Required schema and env assumptions are explicitly verifiable from repo artifacts and operator steps.
    - Launch-blocker checks for DB and environment readiness are current and actionable.
  artifacts:
    - LAUNCH_CHECKLIST.md
    - IMPLEMENTATION_STATUS.md
    - supabase/schema.sql
  key_links:
    - Launch checklist items match the current code and schema expectations.
    - Beta-critical env and schema verification steps are documented or scripted in one obvious place.
---

<objective>
Make database and environment consistency explicit enough for real beta testing, and turn the launch checklist into a current verification tool instead of a stale audit note.

Purpose: If schema and env expectations drift, the beta can fail in ways users experience as broken trust.
Output: Updated launch verification artifacts, reduced env ambiguity, and a clearer path for operators to validate the active environment before inviting real users.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-beta-hardening/02-CONTEXT.md
@SPARQ_MASTER_SPEC.md
@IMPLEMENTATION_STATUS.md
@LAUNCH_CHECKLIST.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-beta-stabilization/01-01-SUMMARY.md
@.planning/phases/01-beta-stabilization/01-02-SUMMARY.md
@.planning/phases/01-beta-stabilization/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align schema and env expectations with the active beta path</name>
  <files>supabase/schema.sql, supabase/migrations, src/lib/api-config.ts, src/integrations/supabase/client.ts, .env.example</files>
  <read_first>SPARQ_MASTER_SPEC.md, IMPLEMENTATION_STATUS.md, LAUNCH_CHECKLIST.md, .planning/codebase/STRUCTURE.md</read_first>
  <action>Audit beta-critical database and environment assumptions. Remove or quarantine remaining Vite-era env usage that still confuses the beta path, make the supported env contract explicit, and ensure the repo’s schema artifacts and migration notes reflect what the current beta code expects. Do not try to redesign the full database story; focus on the tables, columns, and keys that can block real-user testing.</action>
  <verify>rg -n "import\\.meta\\.env|VITE_" src scripts && rg -n "daily_sessions|user_preferences|analytics_events|outcome_assessments|conflict_episodes" supabase/schema.sql supabase/migrations</verify>
  <acceptance_criteria>
    - beta-critical env keys and their supported sources are explicit
    - legacy env usage that can confuse the beta path is removed or clearly fenced off
    - schema artifacts and notes cover the launch-blocker tables and daily-session expectations
  </acceptance_criteria>
  <done>Database and environment expectations for beta are explicit, current, and testable.</done>
</task>

<task type="auto">
  <name>Task 2: Turn launch blockers into current verification steps</name>
  <files>LAUNCH_CHECKLIST.md, IMPLEMENTATION_STATUS.md, scripts</files>
  <read_first>LAUNCH_CHECKLIST.md, IMPLEMENTATION_STATUS.md, .planning/codebase/CONCERNS.md</read_first>
  <action>Update `LAUNCH_CHECKLIST.md` so it reflects the codebase after Phase 1 and clearly distinguishes what can be checked locally, what requires live Supabase or deployment access, and what still blocks real user testing. Add lightweight scripts or command recipes where they reduce operator ambiguity, and update `IMPLEMENTATION_STATUS.md` where the old snapshot would mislead the next round of beta work.</action>
  <verify>rg -n "BLOCKING|Important|Working|daily_sessions|user_preferences|analytics_events" LAUNCH_CHECKLIST.md IMPLEMENTATION_STATUS.md</verify>
  <acceptance_criteria>
    - launch blockers are current, actionable, and tied to actual code paths
    - local versus live-environment checks are clearly separated
    - the checklist no longer points testers at outdated assumptions from before Phase 1
  </acceptance_criteria>
  <done>The launch checklist is usable as an operational beta-readiness tool.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes or reports only pre-existing unrelated warnings
- [ ] Any new verification script or command documented in this plan runs successfully in the local repo context
- [ ] `LAUNCH_CHECKLIST.md` and `IMPLEMENTATION_STATUS.md` reflect the current beta path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- DB and env assumptions are clear enough for real user testing preparation
  </success_criteria>

<output>
After completion, create `.planning/phases/02-beta-hardening/02-01-SUMMARY.md`
</output>
